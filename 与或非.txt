按位与
判定是否存在某个值
按位或
增加某个值

select 支持平台最多　windows Linux Unix
        最多支持的IO数量 1024
        效率一般
poll:支持Linux Unix
     　无上限
       效率一般
epoll:支持Linux
       无上限
       效率较高

epoll效率高的原因:  select/poll　　将关注的IO映射到操作系统当中(类似于拷贝　告知操作系统
轮训监控　不断遍历  发现就绪IO　　要求应用层遍历　找到就绪IO

每一次while True  都要提交到操作系统一遍

epoll
register
直接把IO提交给操作系统,开辟空间

每次poll不会提交操作系统映射

操作系统发现就绪的IO　　直接告诉应用层哪个IO就绪　　不需要再遍历一遍

epoll效率高  监控的io比select多　　触发方式比poll多

IO如果没有处理则不会持续提醒

多了触发方式　　
只是更多选择模式

水平触发
连续不断的提示就绪
边缘触发　
设置为边缘触发
等待下次再有消息发送的时候再提示
EPOLLET

升级HTTP_SERVER

从客户端(浏览器)接收请求
组织响应内容
将响应发送给浏览器

使用IO多路服用　形成IO的并发操作处理
使用类进行封装
网页有多个网页